# Copied to .env by `setup/lab.sh init`, which generates a random password.
# .env is gitignored — no credential ever reaches the repository.
SPLUNK_PASSWORD=
LAB_CONTAINER=splunk-lab
LAB_PORT=8010
LAB_MGMT_PORT=8089
LAB_MEMORY=2600m
